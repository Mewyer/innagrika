🚜 Цифровой Двойник Сельскохозяйственного Участка
Агробарбосики
📜 Описание Проекта

Этот проект представляет собой демонстрационный прототип Цифрового Двойника (Digital Twin) сельскохозяйственного участка. Основная цель — создать интерактивную 3D-модель (визуализируемую в Varwin XRMS), которая калибруется и управляется реальными и смоделированными данными, поступающими с периферийных (Edge) устройств.

Система объединяет локальный сбор данных (Edge Server) с их последующей обработкой, симуляцией и 3D-визуализацией (Varwin XRMS).
💡 Архитектура Системы

Система построена по модели «Облако + Edge-устройства + Клиентский интерфейс + Varwin XRMS».
Компоненты
Компонент	Роль	Протоколы	Файлы
IoT-устройство (Wokwi)	Эмуляция сбора локальных сенсорных данных (T, H, Soil Moisture).	MQTT	(Симуляция в Wokwi)
Edge Server	Прием, агрегация и обработка RAW-данных. Вычисление параметров для симуляционной модели и отправка результатов в Облако.	MQTT (вход), WebSocket (выход)	main.py
Cloud Server	Хранение и предоставление агрегированных и симуляционных данных для 3D-визуализации.	WebSocket (вход), HTTP/API (выход)	(Предполагается внешний Cloud Server)
Varwin XRMS	Интерактивная 3D-визуализация цифрового двойника. Отображение рельефа, сенсорных данных и коммуникаций.	Внутренний API/SDK	(Внешняя платформа)
Модуль ЦМР	Построение и экспорт 3D-модели рельефа на основе исходных JSON-данных.	Локальные файлы (.obj, .stl)	lol.py
Протоколы Передачи Данных

    MQTT (IoT → Edge): Используется для надежной, легковесной передачи сырых (raw) данных с контроллера (Wokwi) на локальный Edge Server (main.py).

        Топики: innagrika/field/raw/temperature, innagrika/field/raw/humidity, и т.д.

    WebSocket (Edge → Cloud): Используется для постоянного двустороннего соединения и передачи агрегированных результатов симуляции (не RAW-данных) с Edge Server в Cloud Server для их использования в цифровом двойнике.

    Локальный Обмен/API (Модуль ЦМР → Varwin): Файлы рельефа (.obj, .stl) экспортируются локально для последующей загрузки в среду Varwin XRMS.

🛠️ Подготовительный Этап (Реализация)
1. Прототип Обмена Данными (Edge Server: main.py)

Модуль main.py реализует Edge Server, который:

    Подключается к MQTT Broker (test.mosquitto.org) и подписывается на RAW-топики сенсоров.

    Агрегирует данные в буфере (реализовано через collections.deque).

    Каждые Config.AGGREGATION_WINDOW секунд (по умолчанию 10 секунд) вычисляет агрегированное значение.

    Отправляет агрегированные данные (которые могут служить параметрами для симуляционной модели) на Cloud Server через WebSocket.

Запуск Веб-интерфейса
```
python cloud_server.py
```

Запуск Edge Server
```
python main.py
```
(Перед запуском убедитесь, что ваш Cloud Server запущен и доступен по адресу, указанному в Config.WEBSOCKET_SERVER)
2. Модуль Построения Карты Рельефа (ЦМР: lol.py)

Модуль lol.py выполняет построение Цифровой Модели Рельефа (ЦМР):

    Загрузка данных: Читает массив координат и высот из JSON-файла.

    Интерполяция: Использует scipy.interpolate.RectBivariateSpline для создания 2D-сетки высот заданного разрешения (по умолчанию 200x200).

    Визуализация: Генерирует тепловую карту (heightmap_heatmap.png) и 3D-поверхность (heightmap_3d.png).

    Экспорт в 3D: Экспортирует интерполированную сетку в форматы .obj и .stl с помощью библиотеки trimesh, совместимые с 3D-средами, такими как Varwin.

Запуск Модуля ЦМР
```
python lol.py
```
(Требуется наличие входного JSON-файла с данными высот и установка библиотек: numpy, scipy, matplotlib, trimesh)
🧩 Функциональность Цифрового Двойника (Varwin)

В среде Varwin XRMS должна быть реализована следующая функциональность:
1. Подсистема Визуализации

    Границы и Рельеф: Импорт и отображение 3D-модели рельефа (.obj/.stl из lol.py).

    Сенсорные Показатели: Отображение смоделированных или агрегированных сенсорных показателей (T, H, Влажность Почвы) в виде цветовой кодировки (тепловых карт) или динамических меток.

2. Карта Коммуникаций

    Отображение Слоев: Интерактивное отображение дополнительных слоев на 3D-рельефе:

        Дренажные Системы: Проектирование и визуализация размещения в низинах (на основе анализа рельефа).

        Системы Орошения: Проектирование и визуализация размещения поливальных установок на возвышенностях.

3. Модуль Генерации/Симуляции

    Калибровка Модели: Параметры внутренней математической модели Varwin (например, скорости испарения, поглощения влаги) должны динамически калиброваться на основе агрегированных данных, поступающих из Edge Server.

    Симуляция Влаги: Моделирование распределения влаги на поле с учетом:

        Текущего рельефа (влага скапливается в низинах).

        Влияния спроектированных дренажных и поливальных систем.

    Внутренний API/Модуль Поступления Данных: Реализация точки приема данных внутри Varwin для получения смоделированных JSON-структур.

🚀 Как Запустить Проект

    Настройка Python-окружения: Установите зависимости для main.py и lol.py.
    Bash

    pip install paho-mqtt websocket-client numpy scipy matplotlib trimesh

    Запуск Cloud Server: Запустите ваш Cloud Server (если он не входит в проект).

    Запуск Edge Server: Запустите main.py.

    Запуск Wokwi Симулятора: Запустите симуляцию контроллера, передающего данные на test.mosquitto.org.

    Генерация 3D Рельефа: Запустите lol.py для создания файлов heightmap.obj и heightmap.stl.

    Настройка Varwin XRMS:

        Импортируйте файлы .obj или .stl в сцену.

        Настройте скрипты для приема данных по WebSocket и визуализации динамических слоев (сенсоры, влага, коммуникации).

📝 Авторы

    <Агробарбосики>
    <Гарипов Рузаль, Шайдуллин Ислам, Галиев Ислам, Файзуллин Шамиль>
